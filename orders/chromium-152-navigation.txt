# A top-level navigation.
#
# Measured 2026-09-08 from Chrome 152, Edge 152 and Brave 152 on Windows,
# each started headless, against local servers that keep headers in arrival
# order: plain HTTP/1.1, HTTP/1.1 over TLS, and HTTP/2 with the HPACK block
# decoded field by field. All three browsers sent this order on all three.
#
# One file covers both protocols because the check skips any header the
# request or this file lacks: over HTTP/1.1 the browser sends host and
# connection and no priority, over HTTP/2 it sends priority and no host or
# connection. Over HTTP/2 the pseudo-headers come first, as
# :method :authority :scheme :path; this checker does not read them.
# sec-gpc is Brave's and the other two do not send it.
#
# referer was added 2026-09-15 from Chrome 152 and Edge 153, on a navigation
# a page started itself: it sits after sec-fetch-dest, the same place the
# subresource file has it. That navigation carries no sec-fetch-user, which
# only a user activated one sends, so the two were not seen together.
#
# Re-measured 2026-09-15 across majors: Opera GX on Chromium 151, Chrome 152,
# and Edge and Brave 153 all send this order character for character, with
# sec-gpc still Brave's alone. So the 152 in the file name is where it was
# first measured and not the limit of what it covers.
#
# Not measured: any major below 151 or above 153, any other platform. A proxy
# or a HAR export
# may not keep the order the browser used. Hold a request against this file
# only when the order it shows is the order that reached the wire.
host
connection
sec-ch-ua
sec-ch-ua-mobile
sec-ch-ua-platform
upgrade-insecure-requests
user-agent
accept
sec-gpc
sec-fetch-site
sec-fetch-mode
sec-fetch-user
sec-fetch-dest
referer
accept-encoding
accept-language
priority
