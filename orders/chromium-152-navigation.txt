# A top-level navigation over HTTP/1.1.
#
# Measured 2026-09-08 against a local server that keeps headers in arrival
# order, from Chrome 152, Edge 152 and Brave 152 on Windows, each started
# headless. All three sent this order. sec-gpc is Brave's and the other two
# do not send it; the check skips any header the request or this file
# lacks, so the file fits a request without host, connection or sec-gpc
# just as well.
#
# Not measured: HTTP/2, where host and connection are not sent and priority
# is; any other major; any other platform. A proxy or a HAR export may not
# keep the order the browser used. Hold a request against this file only
# when the order it shows is the order that reached the wire.
host
connection
sec-ch-ua
sec-ch-ua-mobile
sec-ch-ua-platform
upgrade-insecure-requests
user-agent
accept
sec-gpc
sec-fetch-site
sec-fetch-mode
sec-fetch-user
sec-fetch-dest
accept-encoding
accept-language
