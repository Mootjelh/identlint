# A request a page makes: an image (sec-fetch-mode no-cors), a fetch()
# (sec-fetch-mode cors) and the favicon all use this order.
#
# Measured 2026-09-08 the same way as chromium-152-navigation.txt: Chrome,
# Edge and Brave 152 on Windows, headless, against local servers keeping
# arrival order, over plain HTTP/1.1, HTTP/1.1 over TLS and HTTP/2. The
# client hints move between the two kinds of request: a navigation sends
# sec-ch-ua first, a subresource sends sec-ch-ua-platform first with
# user-agent between it and sec-ch-ua.
#
# As in the navigation file, host and connection are HTTP/1.1 only and
# priority is HTTP/2 only, and the check skips whichever the request lacks.
# sec-gpc is Brave's.
#
# Re-measured 2026-09-15 across majors: Opera GX on Chromium 151, Chrome 152
# and Edge 153 send this order character for character. So the 152 in the file
# name is where it was first measured and not the limit of what it covers.
#
# Not measured: scripts and styles; any major below 151 or above 153; any
# other platform.
host
connection
sec-ch-ua-platform
user-agent
sec-ch-ua
sec-ch-ua-mobile
accept
sec-gpc
sec-fetch-site
sec-fetch-mode
sec-fetch-dest
referer
accept-encoding
accept-language
priority
